Function completions are working, next...
- rename server/hover.go and functions inside to use word "doc"/"documentation"
  instead of "hover", as that file now has a more generic usecase.
- Ditto server/hover package - we can use that as a store of all docstrings.
